<van-calendar show-title="{{ false }}" poppable="{{ false }}" min-date="{{ minDate }}" max-date="{{ maxDate }}" show-confirm="{{ false }}" formatter="{{ formatter }}" />
<view class="zwqd-box">
  <iconfont name='jifenqiandao' class="icon_tap" size='120'></iconfont>
  <view>点击签到</view>
</view>